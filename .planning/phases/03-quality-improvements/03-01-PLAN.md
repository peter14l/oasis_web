---
phase: 03-quality-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
autonomous: true
requirements:
  - Q-TS-01
  - Q-TS-02
user_setup: []

must_haves:
  truths:
    - "TypeScript is installed and configured for the project"
    - "tsconfig.json has proper Vite/React settings"
    - "All JSX files can be renamed to TSX"
  artifacts:
    - path: "package.json"
      provides: "TypeScript dependencies installed"
      contains: "typescript"
    - path: "tsconfig.json"
      provides: "TypeScript configuration for Vite + React"
      contains: "jsx"
  key_links:
    - from: "tsconfig.json"
      to: "Vite"
      via: "moduleResolution: bundler"
      pattern: "bundler"
---

<objective>
Add TypeScript support to the project for type safety.

Purpose: Convert all JavaScript/JSX files to TypeScript/TSX for compile-time type checking and better developer experience.
Output: TypeScript configured, ready for file conversion
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md

# Phase context
Phase 3 goal: Quality Improvements
- Add TypeScript for type safety (this plan)
- Implement Sentry error tracking (plan 03-02)
- Add lazy loading for routes (plan 03-03)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install TypeScript dependencies</name>
  <files>package.json</files>
  <action>
    Install TypeScript and type definitions:
    - Run: npm install -D typescript @types/react @types/react-dom @types/node
    - Update package.json scripts to include type checking: "typecheck": "tsc --noEmit"
  </action>
  <verify>
    <automated>npm list typescript @types/react @types/react-dom</automated>
  </verify>
  <done>TypeScript and type definitions installed</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tsconfig.json configuration</name>
  <files>tsconfig.json</files>
  <action>
    Create tsconfig.json with:
    - target: ES2020
    - lib: ES2020, DOM, DOM.Iterable
    - module: ESNext
    - moduleResolution: bundler
    - jsx: react-jsx
    - strict: true
    - noUnusedLocals: true
    - noUnusedParameters: true
    - isolatedModules: true
    - noEmit: true
    - allowImportingTsExtensions: true
    - resolveJsonModule: true
    - skipLibCheck: true
    - types: vite/client
    - include: src/**/*, vite.config.js
    
    Also create src/vite-env.d.ts for Vite type declarations:
    ```typescript
    /// <reference types="vite/client" />
    ```
  </action>
  <verify>
    <automated>npx tsc --version</automated>
  </verify>
  <done>tsconfig.json created with proper Vite/React settings</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client→TypeScript compiler} | TypeScript runs locally, no runtime trust issues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | tsconfig.json | accept | TypeScript config is local-only, no security impact |
</threat_model>

<verification>
- TypeScript installed: `npm list typescript` shows package
- Configuration valid: `tsc --version` works
</verification>

<success_criteria>
- TypeScript and type definitions installed
- tsconfig.json created with proper Vite/React settings
- Ready for file conversion in Plan 03-01 (Part 2)
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-improvements/03-01-PLAN.md` summary.
</output>
