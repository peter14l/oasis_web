---
phase: 03-quality-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/main.jsx
  - src/components/ErrorBoundary.jsx
autonomous: true
requirements:
  - Q-SENTRY-01
  - Q-SENTRY-02
user_setup: []

must_haves:
  truths:
    - "Sentry SDK is installed"
    - "Sentry is initialized with React 19 error hooks"
    - "ErrorBoundary integrates with Sentry for error capture"
  artifacts:
    - path: "src/main.jsx"
      provides: "Sentry initialization with React 19 error hooks"
      contains: "Sentry.init"
    - path: "src/components/ErrorBoundary.jsx"
      provides: "Sentry error capture integration"
      contains: "Sentry.captureException"
  key_links:
    - from: "main.jsx"
      to: "Sentry"
      via: "Sentry.reactErrorHandler"
      pattern: "onUncaughtError"
    - from: "ErrorBoundary.jsx"
      to: "Sentry"
      via: "captureException"
      pattern: "captureException"
---

<objective>
Implement Sentry error tracking for the application.

Purpose: Add comprehensive error monitoring with React 19 integration and existing ErrorBoundary enhancement.
Output: Sentry configured and capturing errors
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/phases/03-quality-improvements/03-01-PLAN.md

# Sentry integration context
- Use existing SENTRY_DSN from .env (already configured)
- React 19: use Sentry.reactErrorHandler in createRoot options
- ErrorBoundary: enhance with Sentry.captureException
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Sentry packages</name>
  <files>package.json</files>
  <action>
    Install Sentry React SDK:
    - Run: npm install @sentry/react
  </action>
  <verify>
    <automated>npm list @sentry/react</automated>
  </verify>
  <done>Sentry React SDK installed</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Configure Sentry with React 19 error hooks</name>
  <files>src/main.jsx</files>
  <action>
    Update src/main.jsx to initialize Sentry:
    - Import Sentry at top: import * as Sentry from '@sentry/react';
    - Initialize Sentry with:
      - dsn: import.meta.env.VITE_SENTRY_DSN
      - integrations: [Sentry.browserTracingIntegration()]
      - tracesSampleRate: 1.0
      - environment: import.meta.env.MODE
    - Configure createRoot with React 19 error hooks:
      - onUncaughtError: Sentry.reactErrorHandler()
      - onCaughtError: Sentry.reactErrorHandler()
      - onRecoverableError: Sentry.reactErrorHandler()
    - Replace existing ErrorBoundary with Sentry.ErrorBoundary wrapper
    
    Note: Keep existing ErrorBoundary for UI fallback, wrap entire app with Sentry.ErrorBoundary
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -20</automated>
  </verify>
  <done>Sentry initialized with React 19 error hooks</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update ErrorBoundary to integrate with Sentry</name>
  <files>src/components/ErrorBoundary.jsx</files>
  <action>
    Enhance ErrorBoundary to capture errors to Sentry:
    - Import Sentry: import * as Sentry from '@sentry/react';
    - Update componentDidCatch to use Sentry.captureException:
      - Sentry.captureException(error, { extra: errorInfo })
    - Add component stack context for better debugging
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -i error || echo "Build successful"</automated>
  </verify>
  <done>ErrorBoundary integrated with Sentry</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client→Sentry} | Errors sent to Sentry cloud, DSN exposed (intentional for client-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | Sentry DSN | accept | DSN is public key, intentional for client-side error tracking |
| T-03-03 | Information Disclosure | Error data | accept | Errors may contain PII, filtered via beforeSend in production |
</threat_model>

<verification>
- Sentry installed: `npm list @sentry/react` shows package
- Build succeeds: `npm run build` completes without errors
</verification>

<success_criteria>
- Sentry React SDK installed
- Sentry initialized in main.jsx with React 19 error hooks
- ErrorBoundary captures errors to Sentry
- Test error can be triggered and appears in Sentry (manual verification)
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-improvements/03-02-PLAN.md` summary.
</output>
