---
phase: 03-quality-improvements
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/App.jsx
  - src/pages/*.tsx
autonomous: true
requirements:
  - Q-LAZY-01
  - Q-LAZY-02
user_setup: []

must_haves:
  truths:
    - "All route components are lazy-loaded using React.lazy"
    - "Routes are wrapped in Suspense with fallback"
    - "Build output shows code splitting chunks"
  artifacts:
    - path: "src/App.jsx"
      provides: "Lazy-loaded routes with Suspense"
      contains: "React.lazy"
    - path: "src/pages/*.tsx"
      provides: "All page components as lazy imports"
      contains: "default export"
  key_links:
    - from: "App.jsx"
      to: "React.lazy"
      via: "Suspense boundary"
      pattern: "lazy.*import"
---

<objective>
Implement lazy loading for all routes to enable code splitting.

Purpose: Reduce initial bundle size by lazy-loading all route components.
Output: All routes lazy-loaded, code splitting active
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/03-quality-improvements/03-01-PLAN.md
@.planning/phases/03-quality-improvements/03-02-PLAN.md

# Route context from ARCHITECTURE.md
Routes in App.jsx:
- / → Home
- /features → Features
- /pricing → Pricing
- /privacy → Privacy
- /login → Login
- /signup → SignUp
- /checkout → Checkout
- /success → Success
- /moment/:id → SharedMoment
- /reset-password → ResetPasswordHandler
- /profile → Profile
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert all page files to TSX</name>
  <files>
    - src/pages/Home.jsx
    - src/pages/Features.jsx
    - src/pages/Pricing.jsx
    - src/pages/Privacy.jsx
    - src/pages/Login.jsx
    - src/pages/SignUp.jsx
    - src/pages/Checkout.jsx
    - src/pages/Success.jsx
    - src/pages/SharedMoment.jsx
    - src/pages/Profile.jsx
  </files>
  <action>
    Rename all page JSX files to TSX and add type annotations:
    - Home.jsx → Home.tsx
    - Features.jsx → Features.tsx
    - Pricing.jsx → Pricing.tsx
    - Privacy.jsx → Privacy.tsx
    - Login.jsx → Login.tsx
    - SignUp.jsx → SignUp.tsx
    - Checkout.jsx → Checkout.tsx
    - Success.jsx → Success.tsx
    - SharedMoment.jsx → SharedMoment.tsx
    - Profile.jsx → Profile.tsx
    
    For each file:
    - Rename file extension
    - Add proper type annotations for component props
    - Add return type annotations
    - Fix any TypeScript errors (types for useParams, useLocation, etc.)
    
    Note: Keep ResetPasswordHandler inline in App.tsx (too small to lazy-load)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All page files converted to TypeScript</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert component files to TSX</name>
  <files>
    - src/components/Navbar.jsx
    - src/components/Footer.jsx
    - src/components/RazorpayPayment.jsx
    - src/components/PayPalPayment.jsx
    - src/components/PayUPayment.jsx
  </files>
  <action>
    Rename component files and add types:
    - Navbar.jsx → Navbar.tsx
    - Footer.jsx → Footer.tsx
    - RazorpayPayment.jsx → RazorpayPayment.tsx
    - PayPalPayment.jsx → PayPalPayment.tsx
    - PayUPayment.jsx → PayUPayment.tsx
    
    Add proper type annotations for props, events, and Supabase types
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All component files converted to TypeScript</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Convert entry files to TSX</name>
  <files>
    - src/App.jsx
    - src/main.jsx
    - src/supabaseClient.js
  </files>
  <action>
    Rename and convert entry files:
    - App.jsx → App.tsx
    - main.jsx → main.tsx
    - supabaseClient.js → supabaseClient.ts
    
    Add proper types for React Router, Supabase client
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Entry files converted to TypeScript</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Implement lazy loading in App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Implement lazy loading for all routes:
    - Use React.lazy() for each page component import
    - Wrap Routes in Suspense with a loading fallback
    - Keep ResetPasswordHandler inline (too small)
    
    Example pattern:
    ```typescript
    const Home = lazy(() => import('./pages/Home'));
    // ... all other pages
    
    function App() {
      return (
        <ErrorBoundary>
          <Router>
            <Suspense fallback={<LoadingSpinner />}>
              <Routes>
                <Route path="/" element={<Home />} />
                // ... all routes
              </Routes>
            </Suspense>
          </Router>
        </ErrorBoundary>
      );
    }
    ```
    
    Create a simple LoadingSpinner fallback component for Suspense
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(chunk|lazy)" || echo "Build contains code splitting"</automated>
  </verify>
  <done>Lazy loading implemented for all routes</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Verify TypeScript build passes</name>
  <files>
    - package.json
    - tsconfig.json
  </files>
  <action>
    Run final TypeScript verification:
    - Run: npx tsc --noEmit
    - Fix any remaining type errors
    - Ensure strict mode passes
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript build passes without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {lazy-loaded chunks} | Chunks loaded on demand, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | Lazy loading | accept | Chunk loading failure shows fallback, minimal impact |
</threat_model>

<verification>
- TypeScript passes: `npx tsc --noEmit` completes without errors
- Lazy loading works: Build output shows separate chunks
- All routes render: Dev server loads each route correctly
</verification>

<success_criteria>
- All 27 JSX files converted to TSX (11 pages + 5 components + 2 entry + 1 utility + test files)
- TypeScript strict mode passes
- Lazy loading implemented for all 10 page routes
- Build succeeds with code splitting
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-improvements/03-03-PLAN.md` summary.
</output>
