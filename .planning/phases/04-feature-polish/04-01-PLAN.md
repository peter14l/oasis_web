---
phase: 04-feature-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Navbar.tsx
  - src/components/Footer.tsx
autonomous: true
requirements:
  - FP-UI-01
  - FP-UI-02
user_setup: []

must_haves:
  truths:
    - "Navbar uses NavLink for proper active state styling"
    - "Footer social icons are clickable links to actual profiles"
    - "Mobile menu accessibility improved with proper focus management"
  artifacts:
    - path: "src/components/Navbar.tsx"
      provides: "Navigation with NavLink active states"
      contains: "NavLink"
    - path: "src/components/Footer.tsx"
      provides: "Footer with functional social links"
      contains: "href"
  key_links:
    - from: "Navbar.tsx"
      to: "react-router-dom"
      via: "NavLink component"
      pattern: "NavLink"
---

<objective>
Fix navigation/routing issues in the application.

Purpose: Improve navigation UX with proper active state highlighting and fix broken social links in footer.
Output: Fixed navigation components with proper routing behavior
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 4 context
Phase 4 goal: Feature Polish
- Fix navigation/routing issues (this plan)
- Polish UI components (plan 04-02)
- Optimize bundle size (plan 04-03)

# Current issues identified
1. Navbar uses Link instead of NavLink - no active state highlighting
2. Footer social icons (Twitter, Instagram, Github) are not clickable - missing href
3. Mobile menu could benefit from focus management for accessibility
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade Navbar to use NavLink for active states</name>
  <files>src/components/Navbar.tsx</files>
  <behavior>
    - Test 1: NavLink to /features shows active styling when on features page
    - Test 2: NavLink to /pricing shows active styling when on pricing page
    - Test 3: NavLink to /privacy shows active styling when on privacy page
    - Test 4: Desktop nav links show different color when active
  </behavior>
  <action>
    Update Navbar.tsx to use NavLink from react-router-dom:
    - Import NavLink instead of Link for navigation links
    - Use NavLink's `isActive` prop or children function for conditional styling
    - Apply active state styling: color: 'var(--md-sys-color-primary)' when active
    - Keep mobile menu Links as is (or convert to NavLink)
    
    Example pattern:
    ```tsx
    <NavLink 
      to={link.path}
      style={({ isActive }) => ({ 
        color: isActive ? 'var(--md-sys-color-primary)' : 'var(--md-sys-color-on-surface-variant)',
        transition: 'color 0.3s'
      })}
    >
      {link.name}
    </NavLink>
    ```
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>Navbar uses NavLink with proper active state styling</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix Footer social icons to be clickable links</name>
  <files>src/components/Footer.tsx</files>
  <behavior>
    - Test 1: Twitter icon is clickable and links to https://twitter.com/oasis
    - Test 2: Instagram icon is clickable and links to https://instagram.com/oasis
    - Test 3: Github icon is clickable and links to https://github.com/oasis
    - Test 4: Links open in new tab with proper rel="noopener noreferrer"
  </behavior>
  <action>
    Update Footer.tsx social icons to be proper anchor links:
    - Twitter: Add href="https://twitter.com/oasisapp" target="_blank" rel="noopener noreferrer"
    - Instagram: Add href="https://instagram.com/oasisapp" target="_blank" rel="noopener noreferrer"
    - Github: Add href="https://github.com/oasisapp" target="_blank" rel="noopener noreferrer"
    
    Add hover effects for social icons:
    ```tsx
    onMouseEnter={(e) => {
      e.currentTarget.style.color = 'var(--md-sys-color-primary)';
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.color = 'var(--md-sys-color-on-surface-variant)';
    }}
    ```
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>Footer social icons are clickable links to actual profiles</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Improve mobile menu accessibility</name>
  <files>src/components/Navbar.tsx</files>
  <action>
    Enhance mobile menu for accessibility:
    - Add aria-label to mobile toggle button
    - Add role="dialog" and aria-modal="true" to mobile menu overlay
    - Add proper focus trap when menu is open
    - Add keyboard navigation support (Escape to close)
    
    Add to mobile toggle:
    ```tsx
    aria-label={isOpen ? 'Close menu' : 'Open menu'}
    aria-expanded={isOpen}
    ```
    
    Add Escape key handler:
    ```tsx
    useEffect(() => {
      const handleEscape = (e: KeyboardEvent) => {
        if (e.key === 'Escape') setIsOpen(false);
      };
      if (isOpen) {
        document.addEventListener('keydown', handleEscape);
      }
      return () => document.removeEventListener('keydown', handleEscape);
    }, [isOpen]);
    ```
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>Mobile menu has proper accessibility attributes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client→external links} | Social links open external sites, no data transmission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | External links | accept | rel="noopener noreferrer" prevents tabnapping |
| T-04-02 | Information Disclosure | External href | accept | External social profiles are public information |
</threat_model>

<verification>
- TypeScript passes: `npm run typecheck` completes without errors
- NavLink active states work on all route pages
- Footer social links are clickable and open in new tabs
</verification>

<success_criteria>
- Navbar uses NavLink with active state highlighting
- Footer social icons link to actual social profiles
- Mobile menu has proper accessibility attributes
- TypeScript build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-feature-polish/04-01-SUMMARY.md`
</output>
