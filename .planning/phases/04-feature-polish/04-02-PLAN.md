---
phase: 04-feature-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/ErrorBoundary.tsx
  - src/pages/Login.tsx
  - src/pages/SignUp.tsx
autonomous: true
requirements:
  - FP-UI-03
  - FP-UI-04
user_setup: []

must_haves:
  truths:
    - "ErrorBoundary shows styled error UI instead of blank screen"
    - "Login/SignUp pages have consistent loading states"
    - "Route transitions are smooth with proper Suspense fallback"
  artifacts:
    - path: "src/components/ErrorBoundary.tsx"
      provides: "Styled error UI with retry button"
      contains: "error reset"
    - path: "src/App.tsx"
      provides: "Improved LoadingSpinner with branding"
      contains: "OASIS"
  key_links:
    - from: "ErrorBoundary.tsx"
      to: "App.tsx"
      via: "Error boundary wraps router"
      pattern: "ErrorBoundary"
---

<objective>
Polish UI components for consistent user experience.

Purpose: Improve visual consistency, loading states, and error handling UI across the application.
Output: Polished UI components with consistent styling
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-feature-polish/04-01-PLAN.md

# UI Polish Context
- ErrorBoundary shows basic error - needs styled UI
- LoadingSpinner in App.tsx is minimal - needs branding
- Login/SignUp pages need consistent loading states
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Style ErrorBoundary with branded error UI</name>
  <files>src/components/ErrorBoundary.tsx</files>
  <behavior>
    - Test 1: ErrorBoundary displays error message to user
    - Test 2: ErrorBoundary shows retry button that calls resetErrorBoundary
    - Test 3: ErrorBoundary has proper styling consistent with app theme
    - Test 4: ErrorBoundary logs error to console for debugging
  </behavior>
  <action>
    Enhance ErrorBoundary component:
    - Add import for useRouteError from react-router-dom (for route errors)
    - Create styled error UI with:
      - Error icon (AlertTriangle from lucide-react)
      - User-friendly error message
      - "Try Again" button that calls resetErrorBoundary()
      - Support both component errors and route errors
    
    Style requirements:
    - Dark theme consistent with app (background: var(--md-sys-color-surface))
    - Glass panel effect
    - Primary color accent for retry button
    - Error icon with warning color
    
    Example structure:
    ```tsx
    function ErrorFallback({ error, resetErrorBoundary }) {
      return (
        <div role="alert" style={{ /* glass panel styles */ }}>
          <AlertTriangle size={48} />
          <h2>Something went wrong</h2>
          <p>{error.message}</p>
          <button onClick={resetErrorBoundary} className="btn btn-primary">
            Try Again
          </button>
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>ErrorBoundary shows styled branded error UI</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Brand LoadingSpinner with Oasis styling</name>
  <files>src/App.tsx</files>
  <action>
    Enhance the LoadingSpinner component in App.tsx:
    - Add "OASIS" branding text or logo
    - Improve spinner animation with brand colors
    - Add subtle pulsing effect
    
    Update styles:
    ```tsx
    function LoadingSpinner() {
      return (
        <div style={{ 
          minHeight: '100vh', 
          display: 'flex', 
          flexDirection: 'column',
          alignItems: 'center', 
          justifyContent: 'center',
          gap: '1.5rem',
          background: '#0a0a0a' 
        }}>
          <span className="text-gradient" style={{ 
            fontSize: '2rem', 
            fontWeight: 900,
            letterSpacing: '0.1em'
          }}>
            OASIS
          </span>
          <div style={{
            width: '48px',
            height: '48px',
            border: '3px solid rgba(208, 188, 255, 0.2)',
            borderTopColor: '#D0BCFF',
            borderRadius: '50%',
            animation: 'spin 1s linear infinite'
          }} />
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>LoadingSpinner has Oasis branding</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add consistent loading states to Login/SignUp</name>
  <files>
    - src/pages/Login.tsx
    - src/pages/SignUp.tsx
  </files>
  <behavior>
    - Test 1: Login shows loading state during authentication
    - Test 2: SignUp shows loading state during registration
    - Test 3: Submit button is disabled during loading
    - Test 4: Loading state shows spinner or "Processing..."
  </behavior>
  <action>
    Add loading state management to Login and SignUp:
    - Add useState for loading: const [loading, setLoading] = useState(false);
    - Set loading=true before async auth calls
    - Set loading=false after completion (both success and error)
    - Disable submit button when loading
    - Show loading indicator (spinner or text)
    
    Login.tsx pattern:
    ```tsx
    const [loading, setLoading] = useState(false);
    
    const handleSubmit = async (e) => {
      e.preventDefault();
      setLoading(true);
      try {
        // auth logic
      } catch (error) {
        // handle error
      } finally {
        setLoading(false);
      }
    };
    
    // In form:
    <button type="submit" disabled={loading}>
      {loading ? 'Signing in...' : 'Sign In'}
    </button>
    ```
    
    Apply same pattern to SignUp.tsx
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>Login and SignUp have consistent loading states</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client UI} | No new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | Loading state | accept | Loading states prevent double-submit, minimal risk |
</threat_model>

<verification>
- TypeScript passes: `npm run typecheck` completes without errors
- ErrorBoundary displays styled error UI on error
- LoadingSpinner shows Oasis branding
- Login/SignUp show loading states
</verification>

<success_criteria>
- ErrorBoundary shows styled error UI with retry button
- LoadingSpinner has Oasis branding
- Login page shows loading state during auth
- SignUp page shows loading state during registration
- TypeScript build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-feature-polish/04-02-SUMMARY.md`
</output>
